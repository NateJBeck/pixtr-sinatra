<a href= "/galleries/new">Create a new gallery</a>
  
<ul>
  <% @galleries.each do |gallery| %>
    <li> <a href="/galleries/<%= gallery.id %>"><%= gallery.name %> </a></li>
  <% end %>
</ul>


<%# don't put spaces around = in a <a href>
  use %= where normally use puts
do not use ruby interpolation, is security issue%>
